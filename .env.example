# EduPortal Local Development Environment Variables
# Copy this file to credentials.php for local use (credentials.php is gitignored)

# Database (override defaults if needed)
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=edu_portal

# SMTP
SMTP_HOST=ssl://smtp.gmail.com
SMTP_PORT=465
SMTP_USER=eduportal.official.pvt@gmail.com
SMTP_PASS=

# Site URL
SITE_URL=http://localhost/Eduportal
