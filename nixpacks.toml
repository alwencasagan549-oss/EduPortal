[phases.setup]
nixPkgs = ["php81", "php81Packages.composer", "php81Extensions.mysqli", "php81Extensions.pdo_mysql", "php81Extensions.gd", "php81Extensions.mbstring"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php -S 0.0.0.0:$PORT"
